[workspace]
members = [
    "packages/*", 
    "contracts/bluna-vault",
    "contracts/liquidation-helpers/mars_helper",
    "contracts/liquidation-helpers/mirror_helper"
    # "contracts/*"
    ]

[profile.release]
rpath = false
lto = true
overflow-checks = true
opt-level = 3
debug = false
debug-assertions = false
codegen-units = 1
panic = 'abort'
incremental = false
